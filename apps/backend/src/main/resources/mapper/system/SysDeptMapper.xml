<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.forge.admin.modules.system.mapper.SysDeptMapper">

    <select id="hasChildren" resultType="int">
        SELECT COUNT(*) FROM sys_dept WHERE parent_id = #{deptId} AND deleted = 0
    </select>

    <select id="hasUsers" resultType="int">
        SELECT COUNT(*) FROM sys_user WHERE dept_id = #{deptId} AND deleted = 0
    </select>

</mapper>
