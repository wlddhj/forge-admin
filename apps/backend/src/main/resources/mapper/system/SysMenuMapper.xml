<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.forge.admin.modules.system.mapper.SysMenuMapper">

    <select id="selectMenusByUserId" resultType="com.forge.admin.modules.system.entity.SysMenu">
        SELECT DISTINCT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.status = 1 AND m.deleted = 0
        ORDER BY m.sort_order
    </select>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <select id="selectMenusByRoleId" resultType="com.forge.admin.modules.system.entity.SysMenu">
        SELECT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.status = 1 AND m.deleted = 0
        ORDER BY m.sort_order
    </select>

    <select id="hasChildren" resultType="int">
        SELECT COUNT(*) FROM sys_menu WHERE parent_id = #{menuId} AND deleted = 0
    </select>

</mapper>
