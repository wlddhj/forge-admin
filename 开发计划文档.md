# 系统管理模块开发计划文档

## 一、项目概述

### 1.1 项目名称
forge-admin - 聚能后台管理系统

### 1.2 项目目标
从 TrendEye 项目中抽取系统管理模块，构建一个独立、可复用的企业级后台管理系统。

### 1.3 功能范围
| 模块 | 功能描述 |
|------|----------|
| 用户管理 | 用户增删改查、状态管理、密码重置、头像上传 |
| 角色管理 | 角色维护、权限分配（菜单权限） |
| 菜单管理 | 菜单树维护、路由配置、按钮权限 |
| 部门管理 | 部门树维护、层级管理 |
| 岗位管理 | 岗位信息维护、与部门关联 |
| 数据字典 | 字典类型管理、字典数据管理 |
| 系统配置 | 系统参数配置、配置分组管理 |
| 附件管理 | 文件上传下载、附件预览 |
| 文件配置 | 存储配置管理（本地/OSS/COS/MinIO） |
| 操作日志 | 操作记录查询、日志清理 |

---

## 二、技术选型

### 2.1 前端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.4.x | 渐进式 JavaScript 框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 5.x | 构建工具 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.4.x | UI 组件库 |
| Axios | 1.x | HTTP 客户端 |
| ECharts | 5.x | 图表库（可选） |

### 2.2 后端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 17 | 开发语言 |
| Spring Boot | 3.2.x | 应用框架 |
| Spring Security | 6.x | 安全框架 |
| MyBatis-Plus | 3.5.x | ORM 框架 |
| MySQL | 8.0.x | 关系型数据库 |
| Redis | 7.x | 缓存中间件 |
| Knife4j | 4.x | API 文档 |
| Hutool | 5.x | 工具类库 |
| Lombok | 1.18.x | 代码简化 |

---

## 三、项目结构

### 3.1 整体目录结构
```
standAdmin/
├── apps/
│   ├── backend/                    # 后端项目
│   │   ├── src/main/java/com/standadmin/
│   │   │   ├── common/             # 公共模块
│   │   │   │   ├── config/         # 配置类
│   │   │   │   ├── exception/      # 异常处理
│   │   │   │   ├── response/       # 统一响应
│   │   │   │   └── utils/          # 工具类
│   │   │   ├── modules/
│   │   │   │   ├── system/         # 系统管理模块
│   │   │   │   └── auth/           # 认证模块
│   │   │   └── forge-adminApplication.java
│   │   ├── src/main/resources/
│   │   │   ├── application.yml
│   │   │   ├── application-dev.yml
│   │   │   └── mapper/             # MyBatis XML
│   │   └── pom.xml
│   └── frontend/                   # 前端项目
│       ├── src/
│       │   ├── api/                # API 接口
│       │   ├── components/         # 组件
│       │   ├── composables/        # 组合式函数
│       │   ├── hooks/              # 自定义钩子
│       │   ├── layouts/            # 布局组件
│       │   ├── router/             # 路由配置
│       │   ├── stores/             # 状态管理
│       │   ├── styles/             # 样式文件
│       │   ├── types/              # 类型定义
│       │   ├── utils/              # 工具函数
│       │   └── views/              # 页面视图
│       ├── public/
│       ├── index.html
│       ├── vite.config.ts
│       ├── tsconfig.json
│       └── package.json
├── doc/                            # 文档
├── scripts/                        # 脚本
└── sql/                            # 数据库脚本
```

### 3.2 后端模块结构
```
modules/system/
├── controller/                     # 控制器层
│   ├── SysUserController.java
│   ├── SysRoleController.java
│   ├── SysMenuController.java
│   ├── SysDeptController.java
│   ├── SysPositionController.java
│   ├── SysDictTypeController.java
│   ├── SysDictDataController.java
│   ├── SysConfigController.java
│   ├── SysAttachmentController.java
│   ├── SysFileConfigController.java
│   └── SysOperationLogController.java
├── entity/                         # 实体类
├── dto/                            # 数据传输对象
│   ├── user/
│   ├── role/
│   ├── menu/
│   ├── dept/
│   ├── position/
│   ├── dict/
│   ├── config/
│   ├── attachment/
│   └── log/
├── service/                        # 服务接口
├── service/impl/                   # 服务实现
├── mapper/                         # 数据访问层
└── convert/                        # 对象转换器
```

---

## 四、数据库设计

### 4.1 核心表结构

#### 用户表 (sys_user)
```sql
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `nickname` varchar(50) DEFAULT NULL COMMENT '昵称',
  `password` varchar(100) NOT NULL COMMENT '密码',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像',
  `dept_id` bigint DEFAULT NULL COMMENT '部门ID',
  `position_id` bigint DEFAULT NULL COMMENT '岗位ID',
  `account_type` tinyint DEFAULT 0 COMMENT '账户类型(0:普通用户 1:管理员)',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `last_login_time` datetime DEFAULT NULL COMMENT '最后登录时间',
  `last_login_ip` varchar(50) DEFAULT NULL COMMENT '最后登录IP',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_dept_id` (`dept_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 角色表 (sys_role)
```sql
CREATE TABLE `sys_role` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `role_name` varchar(50) NOT NULL COMMENT '角色名称',
  `role_code` varchar(50) NOT NULL COMMENT '角色编码',
  `description` varchar(255) DEFAULT NULL COMMENT '描述',
  `is_fixed` tinyint DEFAULT 0 COMMENT '是否固定(0:否 1:是)',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `sort_order` int DEFAULT 0 COMMENT '排序',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';
```

#### 菜单表 (sys_menu)
```sql
CREATE TABLE `sys_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `menu_name` varchar(50) NOT NULL COMMENT '菜单名称',
  `menu_code` varchar(50) DEFAULT NULL COMMENT '菜单编码',
  `parent_id` bigint DEFAULT 0 COMMENT '父菜单ID',
  `route_path` varchar(200) DEFAULT NULL COMMENT '路由路径',
  `component_path` varchar(200) DEFAULT NULL COMMENT '组件路径',
  `redirect_path` varchar(200) DEFAULT NULL COMMENT '重定向路径',
  `icon` varchar(100) DEFAULT NULL COMMENT '图标',
  `sort_order` int DEFAULT 0 COMMENT '排序',
  `menu_type` tinyint DEFAULT 0 COMMENT '类型(0:目录 1:菜单 2:按钮)',
  `permission` varchar(100) DEFAULT NULL COMMENT '权限标识',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `visible` tinyint DEFAULT 1 COMMENT '是否可见(0:否 1:是)',
  `is_external` tinyint DEFAULT 0 COMMENT '是否外链',
  `is_cached` tinyint DEFAULT 0 COMMENT '是否缓存',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  KEY `idx_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';
```

#### 部门表 (sys_dept)
```sql
CREATE TABLE `sys_dept` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `dept_name` varchar(50) NOT NULL COMMENT '部门名称',
  `parent_id` bigint DEFAULT 0 COMMENT '父部门ID',
  `ancestors` varchar(500) DEFAULT NULL COMMENT '祖级列表',
  `leader` varchar(50) DEFAULT NULL COMMENT '负责人',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `phone` varchar(20) DEFAULT NULL COMMENT '电话',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `sort_order` int DEFAULT 0 COMMENT '排序',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  KEY `idx_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='部门表';
```

#### 岗位表 (sys_position)
```sql
CREATE TABLE `sys_position` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `position_name` varchar(50) NOT NULL COMMENT '岗位名称',
  `position_code` varchar(50) NOT NULL COMMENT '岗位编码',
  `dept_id` bigint DEFAULT NULL COMMENT '部门ID',
  `sort_order` int DEFAULT 0 COMMENT '排序',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_position_code` (`position_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='岗位表';
```

#### 字典类型表 (sys_dict_type)
```sql
CREATE TABLE `sys_dict_type` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `dict_name` varchar(50) NOT NULL COMMENT '字典名称',
  `dict_type` varchar(100) NOT NULL COMMENT '字典类型',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `is_system` tinyint DEFAULT 0 COMMENT '是否系统内置',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_dict_type` (`dict_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='字典类型表';
```

#### 字典数据表 (sys_dict_data)
```sql
CREATE TABLE `sys_dict_data` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `dict_type` varchar(100) NOT NULL COMMENT '字典类型',
  `dict_label` varchar(50) NOT NULL COMMENT '字典标签',
  `dict_value` varchar(100) NOT NULL COMMENT '字典值',
  `dict_sort` int DEFAULT 0 COMMENT '排序',
  `css_class` varchar(100) DEFAULT NULL COMMENT '样式属性',
  `list_class` varchar(100) DEFAULT NULL COMMENT '表格回显样式',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  KEY `idx_dict_type` (`dict_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='字典数据表';
```

#### 系统配置表 (sys_config)
```sql
CREATE TABLE `sys_config` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `config_name` varchar(100) NOT NULL COMMENT '配置名称',
  `config_key` varchar(100) NOT NULL COMMENT '配置键',
  `config_value` varchar(500) DEFAULT NULL COMMENT '配置值',
  `config_type` varchar(50) DEFAULT 'text' COMMENT '配置类型(text/number/boolean/json)',
  `config_group` varchar(50) DEFAULT 'system' COMMENT '配置分组',
  `is_system` tinyint DEFAULT 0 COMMENT '是否系统内置',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';
```

#### 文件存储配置表 (sys_file_config)
```sql
CREATE TABLE `sys_file_config` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `config_name` varchar(100) NOT NULL COMMENT '配置名称',
  `storage_type` varchar(20) NOT NULL COMMENT '存储类型(local/aliyun_oss/tencent_cos/minio)',
  `config_json` text COMMENT '配置JSON',
  `is_default` tinyint DEFAULT 0 COMMENT '是否默认',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文件存储配置表';
```

#### 附件表 (sys_attachment)
```sql
CREATE TABLE `sys_attachment` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `file_name` varchar(255) NOT NULL COMMENT '文件名',
  `original_name` varchar(255) NOT NULL COMMENT '原始文件名',
  `file_path` varchar(500) NOT NULL COMMENT '文件路径',
  `file_size` bigint DEFAULT 0 COMMENT '文件大小(字节)',
  `file_type` varchar(50) DEFAULT NULL COMMENT '文件类型',
  `mime_type` varchar(100) DEFAULT NULL COMMENT 'MIME类型',
  `storage_type` varchar(20) DEFAULT 'local' COMMENT '存储类型',
  `biz_type` varchar(50) DEFAULT NULL COMMENT '业务类型',
  `biz_id` bigint DEFAULT NULL COMMENT '业务ID',
  `thumbnail_url` varchar(500) DEFAULT NULL COMMENT '缩略图URL',
  `upload_user_id` bigint DEFAULT NULL COMMENT '上传用户ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  KEY `idx_biz` (`biz_type`, `biz_id`),
  KEY `idx_upload_user` (`upload_user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='附件表';
```

#### 操作日志表 (sys_operation_log)
```sql
CREATE TABLE `sys_operation_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `title` varchar(100) DEFAULT NULL COMMENT '操作标题',
  `business_type` varchar(50) DEFAULT NULL COMMENT '业务类型',
  `request_method` varchar(10) DEFAULT NULL COMMENT '请求方式',
  `request_url` varchar(500) DEFAULT NULL COMMENT '请求URL',
  `operator_id` bigint DEFAULT NULL COMMENT '操作人ID',
  `operator_name` varchar(50) DEFAULT NULL COMMENT '操作人姓名',
  `dept_name` varchar(100) DEFAULT NULL COMMENT '部门名称',
  `operate_ip` varchar(50) DEFAULT NULL COMMENT '操作IP',
  `operate_location` varchar(100) DEFAULT NULL COMMENT '操作地点',
  `request_param` text COMMENT '请求参数',
  `json_result` text COMMENT '响应结果',
  `status` tinyint DEFAULT 1 COMMENT '状态(0:失败 1:成功)',
  `error_msg` text COMMENT '错误信息',
  `operate_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  `cost_time` bigint DEFAULT 0 COMMENT '耗时(毫秒)',
  PRIMARY KEY (`id`),
  KEY `idx_operator` (`operator_id`),
  KEY `idx_operate_time` (`operate_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

#### 用户角色关联表 (sys_user_role)
```sql
CREATE TABLE `sys_user_role` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_role` (`user_id`, `role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';
```

#### 角色菜单关联表 (sys_role_menu)
```sql
CREATE TABLE `sys_role_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `menu_id` bigint NOT NULL COMMENT '菜单ID',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_menu` (`role_id`, `menu_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';
```

---

## 五、API 接口设计

### 5.1 接口规范
- **基础路径**: `/api/system`
- **请求方式**: RESTful 风格
- **响应格式**: JSON

### 5.2 统一响应结构
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 5.3 分页请求结构
```json
{
  "pageNum": 1,
  "pageSize": 10,
  "orderBy": "create_time",
  "isAsc": false
}
```

### 5.4 分页响应结构
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [],
    "total": 100,
    "pageNum": 1,
    "pageSize": 10,
    "pages": 10
  }
}
```

### 5.5 接口清单

#### 用户管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /user/list | 分页查询用户列表 |
| GET | /user/{id} | 获取用户详情 |
| POST | /user | 新增用户 |
| PUT | /user | 更新用户 |
| DELETE | /user/{ids} | 批量删除用户 |
| PUT | /user/{id}/status | 更新用户状态 |
| PUT | /user/{id}/reset-password | 重置密码 |
| PUT | /user/profile | 更新个人信息 |
| PUT | /user/password | 修改密码 |

#### 角色管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /role/list | 分页查询角色列表 |
| GET | /role/all | 获取所有角色 |
| GET | /role/{id} | 获取角色详情 |
| POST | /role | 新增角色 |
| PUT | /role | 更新角色 |
| DELETE | /role/{ids} | 批量删除角色 |
| PUT | /role/{id}/status | 更新角色状态 |
| GET | /role/{id}/menus | 获取角色菜单 |
| PUT | /role/{id}/menus | 分配菜单权限 |

#### 菜单管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /menu/list | 查询菜单列表 |
| GET | /menu/tree | 获取菜单树 |
| GET | /menu/{id} | 获取菜单详情 |
| POST | /menu | 新增菜单 |
| PUT | /menu | 更新菜单 |
| DELETE | /menu/{id} | 删除菜单 |

#### 部门管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /dept/list | 查询部门列表 |
| GET | /dept/tree | 获取部门树 |
| GET | /dept/{id} | 获取部门详情 |
| POST | /dept | 新增部门 |
| PUT | /dept | 更新部门 |
| DELETE | /dept/{id} | 删除部门 |

#### 岗位管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /position/list | 分页查询岗位列表 |
| GET | /position/all | 获取所有岗位 |
| GET | /position/{id} | 获取岗位详情 |
| POST | /position | 新增岗位 |
| PUT | /position | 更新岗位 |
| DELETE | /position/{ids} | 批量删除岗位 |

#### 字典类型管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /dict-type/list | 分页查询字典类型 |
| GET | /dict-type/all | 获取所有字典类型 |
| GET | /dict-type/{id} | 获取字典类型详情 |
| POST | /dict-type | 新增字典类型 |
| PUT | /dict-type | 更新字典类型 |
| DELETE | /dict-type/{ids} | 批量删除字典类型 |

#### 字典数据管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /dict-data/list | 分页查询字典数据 |
| GET | /dict-data/type/{dictType} | 按类型获取字典数据 |
| GET | /dict-data/{id} | 获取字典数据详情 |
| POST | /dict-data | 新增字典数据 |
| PUT | /dict-data | 更新字典数据 |
| DELETE | /dict-data/{ids} | 批量删除字典数据 |

#### 系统配置管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /config/list | 分页查询配置列表 |
| GET | /config/key/{key} | 按键获取配置值 |
| GET | /config/{id} | 获取配置详情 |
| POST | /config | 新增配置 |
| PUT | /config | 更新配置 |
| DELETE | /config/{ids} | 批量删除配置 |

#### 附件管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /attachment/list | 分页查询附件列表 |
| GET | /attachment/{id} | 获取附件详情 |
| POST | /attachment/upload | 上传文件 |
| GET | /attachment/download/{id} | 下载文件 |
| DELETE | /attachment/{ids} | 批量删除附件 |

#### 文件配置管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /file-config/list | 查询文件配置列表 |
| GET | /file-config/{id} | 获取文件配置详情 |
| POST | /file-config | 新增文件配置 |
| PUT | /file-config | 更新文件配置 |
| DELETE | /file-config/{id} | 删除文件配置 |
| PUT | /file-config/{id}/default | 设置默认配置 |

#### 操作日志
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /operation-log/list | 分页查询操作日志 |
| GET | /operation-log/{id} | 获取日志详情 |
| DELETE | /operation-log/clear | 清空日志 |

---

## 六、开发计划

### 6.1 阶段一：项目初始化

#### 后端初始化
- [ ] 创建 Spring Boot 项目 (Java 17)
- [ ] 配置 pom.xml 依赖
- [ ] 配置 application.yml
- [ ] 创建项目基础包结构
- [ ] 配置 MyBatis-Plus
- [ ] 配置 Spring Security
- [ ] 创建统一响应封装
- [ ] 创建全局异常处理
- [ ] 创建通用工具类

#### 前端初始化
- [ ] 创建 Vue3 + Vite 项目
- [ ] 配置 TypeScript
- [ ] 安装配置 Element Plus
- [ ] 配置 Vue Router
- [ ] 配置 Pinia
- [ ] 配置 Axios 封装
- [ ] 创建项目目录结构
- [ ] 创建基础布局组件

### 6.2 阶段二：认证授权模块

#### 后端
- [ ] 实现登录接口
- [ ] 实现 JWT Token 生成和验证
- [ ] 实现 Spring Security 配置
- [ ] 实现用户权限加载
- [ ] 实现接口权限注解

#### 前端
- [ ] 实现登录页面
- [ ] 实现路由守卫
- [ ] 实现 Token 存储和管理
- [ ] 实现登录状态管理

### 6.3 阶段三：系统管理核心模块

#### 用户管理
- [ ] 后端：Entity → Mapper → Service → Controller
- [ ] 前端：API → Types → Views
- [ ] 用户列表（分页、搜索）
- [ ] 用户新增/编辑表单
- [ ] 用户删除（单个/批量）
- [ ] 用户状态切换
- [ ] 密码重置功能
- [ ] 头像上传

#### 角色管理
- [ ] 后端 CRUD 实现
- [ ] 前端页面实现
- [ ] 角色权限分配（菜单树选择）

#### 菜单管理
- [ ] 后端 CRUD 实现（树形结构）
- [ ] 前端页面实现（树形表格）
- [ ] 菜单图标选择器
- [ ] 菜单类型切换

#### 部门管理
- [ ] 后端 CRUD 实现（树形结构）
- [ ] 前端页面实现（树形组件）
- [ ] 部门层级管理

#### 岗位管理
- [ ] 后端 CRUD 实现
- [ ] 前端页面实现
- [ ] 岗位与部门关联

### 6.4 阶段四：字典与配置模块

#### 数据字典
- [ ] 字典类型管理
- [ ] 字典数据管理
- [ ] 字典数据缓存

#### 系统配置
- [ ] 配置项管理
- [ ] 配置分组
- [ ] 配置缓存

### 6.5 阶段五：附件与文件模块

#### 文件存储配置
- [ ] 本地存储配置
- [ ] 阿里云 OSS 配置
- [ ] 腾讯云 COS 配置
- [ ] MinIO 配置

#### 附件管理
- [ ] 文件上传接口
- [ ] 文件下载接口
- [ ] 图片预览功能
- [ ] 附件列表管理

### 6.6 阶段六：操作日志模块

- [ ] 操作日志 AOP 实现
- [ ] 日志列表查询
- [ ] 日志详情查看
- [ ] 日志清理功能

### 6.7 阶段七：优化与完善

- [ ] 代码优化和重构
- [ ] 接口文档完善
- [ ] 单元测试
- [ ] 性能优化
- [ ] 安全加固

---

## 七、迁移指南

### 7.1 从 TrendEye 迁移的关键文件

#### 后端核心文件
```
源路径: /Users/huangjian/workspace/cursor/TrendEye/apps/backend/src/main/java/com/trendeye/
目标路径: /Users/huangjian/workspace/cursor/standAdmin/apps/backend/src/main/java/com/standadmin/

需要迁移的目录:
├── common/                         # 公共模块
│   ├── config/                     # 配置类
│   ├── exception/                  # 异常处理
│   ├── response/                   # 响应封装
│   └── utils/                      # 工具类
├── modules/
│   ├── system/                     # 系统管理模块（核心）
│   └── auth/                       # 认证模块
```

#### 前端核心文件
```
源路径: /Users/huangjian/workspace/cursor/TrendEye/apps/frontend/src/
目标路径: /Users/huangjian/workspace/cursor/standAdmin/apps/frontend/src/

需要迁移的目录:
├── api/system.ts                   # 系统管理 API
├── types/system.ts                 # 系统管理类型
├── views/system/                   # 系统管理页面
├── components/layout/              # 布局组件
└── stores/                         # 状态管理
```

### 7.2 迁移注意事项

1. **包名修改**
   - 将 `com.trendeye` 修改为 `com.forge.admin`

2. **配置修改**
   - 数据库连接配置
   - Redis 连接配置
   - 文件存储路径配置

3. **前端修改**
   - API 基础路径配置
   - 项目名称和标题
   - 移除业务相关代码

4. **数据库**
   - 执行建表 SQL
   - 初始化基础数据（超级管理员、默认角色等）

---

## 八、初始化数据

### 8.1 默认管理员
```sql
-- 默认密码: admin123 (使用 BCrypt 加密)
INSERT INTO `sys_user` (`username`, `nickname`, `password`, `account_type`, `status`)
VALUES ('admin', '超级管理员', '$2a$10$...', 1, 1);
```

### 8.2 默认角色
```sql
INSERT INTO `sys_role` (`role_name`, `role_code`, `description`, `is_fixed`, `status`)
VALUES
('超级管理员', 'SUPER_ADMIN', '拥有所有权限', 1, 1),
('普通用户', 'USER', '普通用户角色', 1, 1);
```

### 8.3 默认菜单
```sql
INSERT INTO `sys_menu` (`menu_name`, `parent_id`, `route_path`, `component_path`, `icon`, `menu_type`, `sort_order`)
VALUES
('系统管理', 0, '/system', 'Layout', 'Setting', 0, 1),
('用户管理', 1, '/system/user', '/views/system/user/index', 'User', 1, 1),
('角色管理', 1, '/system/role', '/views/system/role/index', 'UserFilled', 1, 2),
('菜单管理', 1, '/system/menu', '/views/system/menu/index', 'Menu', 1, 3),
('部门管理', 1, '/system/dept', '/views/system/dept/index', 'OfficeBuilding', 1, 4),
('岗位管理', 1, '/system/position', '/views/system/position/index', 'Briefcase', 1, 5),
('字典管理', 1, '/system/dict', '/views/system/dict-type/index', 'Collection', 1, 6),
('系统配置', 1, '/system/config', '/views/system/config/index', 'Tools', 1, 7),
('附件管理', 1, '/system/attachment', '/views/system/attachment/index', 'Folder', 1, 8),
('操作日志', 1, '/system/log', '/views/system/operation-log/index', 'Document', 1, 9);
```

---

## 九、技术要点

### 9.1 权限控制
- 基于角色的访问控制 (RBAC)
- 菜单权限：控制用户可见菜单
- 按钮权限：控制页面按钮显示
- 接口权限：使用 `@PreAuthorize` 注解

### 9.2 数据权限
- 部门数据权限
- 用户数据权限
- 自定义数据范围

### 9.3 安全措施
- 密码 BCrypt 加密
- JWT Token 认证
- XSS 过滤
- SQL 注入防护
- 接口限流

### 9.4 性能优化
- Redis 缓存字典数据
- Redis 缓存用户权限
- 分页查询优化
- 数据库索引优化

---

## 十、附录

### 10.1 参考项目
- TrendEye: `/Users/huangjian/workspace/cursor/TrendEye`

### 10.2 技术文档
- Spring Boot: https://spring.io/projects/spring-boot
- Vue 3: https://vuejs.org/
- Element Plus: https://element-plus.org/
- MyBatis-Plus: https://baomidou.com/

---

**文档版本**: v1.0
**创建日期**: 2026-02-27
**作者**: Claude Code
